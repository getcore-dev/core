<!DOCTYPE html>
<html>

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
  <link rel="stylesheet" href="/css/navbar.css" />
  <link rel="stylesheet" href="/css/user_profile.css" />
  <link rel="shortcut icon" href="/img/core.png" />
</head>

<style>
  main {
    display: block;
  }

  .follower {
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 10px;
    background-color: var(--light-mode-body-color);
    width: 100%;
    padding: 10px 13px;
  }

  .follower:hover {
    background-color: var(--light-mode-hover-color);
    cursor: pointer;
  }

  .followers-header {
    margin: 13px 10px;
  }

  @media (prefers-color-scheme: dark) {
    .follower {
      background-color: var(--dark-mode-body-color);
    }

    .follower:hover {
      background-color: var(--dark-mode-hover-background-color);
    }

  }
</style>

<body>
  <%- include('partials/navbar', { pagePath: 'updates' }) %>
  <main>
    <h5 class="followers-header">Followers</h5>
    <div class="followers">
      <% followers.forEach((follower) => { %>
      <div class="follower" onclick="window.location.href='/user/<%= follower.username %>'">
        <img class="thumbnail thumbnail-tiny" src="<%= follower.avatar %>" alt="<%= follower.username %>" />
        <div class="follower-info">
          <h3>
            <%= follower.firstname %>
            <%= follower.lastname %>
          </h3>
          <p id="secondary-text">
            @<%= follower.username.toLowerCase() %>
          </p>
        </div>
      </div>
      <% }) %>
    </div>
  </main>
</body>

</html>