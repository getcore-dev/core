<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <link rel="stylesheet" href="/css/navbar.css" />
    <link rel="stylesheet" href="/css/communities.css" />
    <link rel="stylesheet" href="/css/com-btn-container.css" />
    <link rel="shortcut icon" href="/img/core.png" />
    <title>Search Results - <%= searchTerm %></title>
</head>

<body>
    <%- include('partials/navbar', { pagePath: 'search', user: user }) %>

    <div class="container">
        <div id="posts-container" class="column">
            <h2>Search Results for '<%= searchTerm %>'</h2>
            <% if (results.length > 0) { %>
            <div class="posts">
                    <% results.forEach(function(post) { %>
                <div class="post">
                    <div class="post-content">
                    
                    <div class="post-header">
                        <h3><%= post.title %></h3>
                        <span>Posted on: <%= new Date(post.created_at).toLocaleDateString() %></span>
                    </div>
                    <div class="post-content">
                        <p><%= post.content %></p>
                                <% if (post.link) { %>
                        <a href="<%= post.link %>" target="_blank">Read More</a>
                                <% } %>
                    </div>
                    <div class="post-footer">
                        <span class="react-like">👍 <%= post.react_like %></span>
                        <span class="react-love">❤️ <%= post.react_love %></span>
                        <span class="react-curious">🤔 <%= post.react_curious %></span>
                        <span class="react-interesting">💡 <%= post.react_interesting %></span>
                        <span class="react-celebrate">🎉 <%= post.react_celebrate %></span>
                    </div>
                    </div>
                </div>
                    <% }); %>
            </div>
            <% } else { %>
            <p>No results found for '<%= searchTerm %>'.</p>
            <% } %>
        </div>
    </div>
</body>
</html>
