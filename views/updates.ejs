<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>About Core</title>
    <link rel="stylesheet" href="/css/navbar.css" />
    <link rel="stylesheet" href="/css/updates.css" />
    <link rel="shortcut icon" href="img/core.png" />
  </head>
  <style>
  </style>
  <body>
    <%- include('partials/navbar', { pagePath: 'home' }) %>
    <main>
    <div id="commits-container">
      <h1>Core Updates</h1>
      <div id="commits">
        <% for (let i = 0; i < commits.length; i++) { %>
          <div class="commit">
            <div class="commit-header">
              <div class="commit-title">
                <a href="<%= commits[i].html_url %>"><%= commits[i].commit.message %></a>
              </div>
              <div class="commit-date">
                <%= commits[i].commit.author.date %>
              </div>
            </div>
            <div class="commit-body">
              <div class="commit-author">
                <img src="<%= commits[i].author.avatar_url %>" alt="avatar" />
                <div class="commit-author-name">
                  <%= commits[i].commit.author.name %>
                </div>
              </div>
              <div class="commit-stats">
                <div class="commit-stats-item" style="background-color: #112987ae;">
                  <div class="commit-stats-item-value">
                    <%= commits[i].commit.comment_count %>
                  </div>
                </div>

                <div class="commit-stats-item" style="background-color: #12542489;">
                  <span class="material-symbols-outlined">add</span>
                  <div class="commit-stats-item-value">
                    <%= commits[i].linesAdded %>
                  </div>
                </div>
                <div class="commit-stats-item" style="background-color: #7e0d0da7;">
                  <span class="material-symbols-outlined">remove</span>
                  <div class="commit-stats-item-value">
                    <%= commits[i].linesDeleted %>
                  </div>
                </div>
              </div>
            </div>
          </div>
        <% } %>
    </div>
  </main>

    <script>
    </script>
  </body>
</html>
