<div class="flex flex-col items-start gap-2 rounded-lg border p-3 text-left text-sm transition-all hover:bg-accent bg-muted">
  <div class="flex w-full flex-col gap-1">
    <div class="flex items-center">
      <div class="flex items-center gap-2">
        <div class="font-semibold"><%= name %></div>
      </div>
      <div class="ml-auto text-xs text-foreground"><%= timestamp %></div>
    </div>
    <div class="text-xs font-medium"><%= title %></div>
  </div>
  <div class="line-clamp-2 text-xs text-muted-foreground">
    <%= description %>
  </div>
</div>